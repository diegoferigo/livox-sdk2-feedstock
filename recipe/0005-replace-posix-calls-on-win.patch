--- a/sdk_core/logger_handler/file_manager.cpp
+++ b/sdk_core/logger_handler/file_manager.cpp
@@ -26,6 +26,8 @@

 #ifdef WIN32
   #include <direct.h>
+  #include <io.h>
+  #include <windows.h>
 #else
   #include <dirent.h>
   #include <sys/stat.h>
@@ -42,6 +44,28 @@
 namespace livox {
 namespace lidar {

+namespace {
+
+#ifdef WIN32
+int FileAccess(const char* path, int mode) {
+  return _access(path, mode);
+}
+
+int CreateDirectoryIfNeeded(const char* path) {
+  return _mkdir(path);
+}
+#else
+int FileAccess(const char* path, int mode) {
+  return access(path, mode);
+}
+
+int CreateDirectoryIfNeeded(const char* path) {
+  return mkdir(path, 0777);
+}
+#endif
+
+} // namespace
+
 constexpr uint16_t kLengthOfTimeInFilename = 19;
 constexpr bool kFOk = 0;

@@ -137,12 +161,12 @@
         if (filename.empty()) {
           continue;
         }
-        if(access((dir_name + "\\" + filename).c_str(), kFOk) != 0) {
+        if(FileAccess((dir_name + "\\" + filename).c_str(), kFOk) != 0) {
           LOG_WARN("The file to be renamed : {} does not exist ", filename);
           continue;
         }
         std::string file_name_cut = filename.substr(1);
-        if(access((dir_name + "\\" + file_name_cut).c_str(), kFOk) == 0) {
+        if(FileAccess((dir_name + "\\" + file_name_cut).c_str(), kFOk) == 0) {
           if (remove((dir_name + "\\" + file_name_cut).c_str()) != 0) {
             LOG_WARN("Failed to remove the existing file: {}. errno: {}", file_name_cut, errno);
           }
@@ -250,12 +274,12 @@
       if (filename.empty()) {
         continue;
       }
-      if(access((dir_name + "/" + filename).c_str(), kFOk) != 0) {
+      if(FileAccess((dir_name + "/" + filename).c_str(), kFOk) != 0) {
         LOG_WARN("The file to be renamed : {} does not exist ", filename);
         continue;
       }
       std::string file_name_cut = filename.substr(1);
-      if(access((dir_name + "/" + file_name_cut).c_str(), kFOk) == 0) {
+      if(FileAccess((dir_name + "/" + file_name_cut).c_str(), kFOk) == 0) {
         if (remove((dir_name + "/" + file_name_cut).c_str()) != 0) {
           LOG_WARN("Failed to remove the existing file: {}. errno: {}", file_name_cut, errno);
         }
@@ -318,12 +342,12 @@
   if (file_name.at(0) != '.') {
     return false;
   }
-  if(access((dir_name + "/" + file_name).c_str(), kFOk) != 0) {
+  if(FileAccess((dir_name + "/" + file_name).c_str(), kFOk) != 0) {
     LOG_WARN("The file to be renamed : {} does not exist ", file_name);
     return false;
   }
   std::string file_name_cut = file_name.substr(1);
-  if(access((dir_name + "/" + file_name_cut).c_str(), kFOk) == 0) {
+  if(FileAccess((dir_name + "/" + file_name_cut).c_str(), kFOk) == 0) {
     if (remove((dir_name + "/" + file_name_cut).c_str()) != 0) {
       LOG_WARN("Failed to remove the existing file: {}. errno: {}", file_name_cut, errno);
     }
@@ -346,17 +370,12 @@
 }

 bool MakeDirecotory(std::string dir) {
-  int flag = -1;
-#ifdef WIN32
-  flag = mkdir(dir.c_str());
-#else
-  flag = mkdir(dir.c_str(), 0777);
-#endif // WIN32
+  int flag = CreateDirectoryIfNeeded(dir.c_str());
   return (flag == 0);
 }

 bool IsDirectoryExits(std::string dir) {
-  return access(dir.c_str(), 0) == EXIT_SUCCESS;
+  return FileAccess(dir.c_str(), 0) == EXIT_SUCCESS;
 }

 } // namespace lidar
--- a/sdk_core/logger_handler/logger_manager.cpp
+++ b/sdk_core/logger_handler/logger_manager.cpp
@@ -118,1 +118,1 @@
-  if (access(log_root_dir.c_str(), 0) != EXIT_SUCCESS) {
+  if (!IsDirectoryExits(log_root_dir)) {
--- a/sdk_core/upgrade/firmware.cpp
+++ b/sdk_core/upgrade/firmware.cpp
@@ -95,7 +95,8 @@
   if (file_) {
     printf("All firmware data have be read successfully.\n");
   } else {
-    printf("Read firmware fail[%ld]!\n", file_.gcount());
+    printf("Read firmware fail[%lld]!\n",
+           static_cast<long long>(file_.gcount()));
   }

   return true;
